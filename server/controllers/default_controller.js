//require mongoose will always be needed
var mongoose = require('mongoose');
//model that you will use in your controller, if you call on more than one model in your controller, make sure to require all of them!!!
// var Default = mongoose.model('Default');

module.exports = (function(){
  return {

  }
})
